[package]
name = "pygarden"
version = "0.1.0-alpha.0"
edition = "2024"

[dependencies]
bevy = {version = "0.18.1", default-features = false, features = ["2d", "audio", "mp3", "wav"] }
rand = "0.10.0"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
